# xdg-desktop-portal-hyprland
# allow_token_by_default: authorize once locally, then reconnect automatically
# without requesting on-screen confirmation. Required for remote RustDesk.
screencopy {
    allow_token_by_default = true
    max_fps = 30
}
