# Second paccache pass. The unit shipped by pacman-contrib only keeps
# the last 3 versions of what is INSTALLED; this additionally purges entirely
# what is no longer installed, which is what really bloats /var/cache/pacman/pkg.
[Service]
ExecStart=/usr/bin/paccache -ruk0
